"""SDK version string.

The version here is the authoritative source used for the default
``User-Agent`` header. Keep it in sync with ``pyproject.toml`` — the
test suite asserts they match.
"""

VERSION = "1.0.0rc1"
